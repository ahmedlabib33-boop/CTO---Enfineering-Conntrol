# API

Run the application and open `/docs` for generated OpenAPI documentation.

Demo authentication:
- username: `admin`
- password: `admin`

The login returns a bearer token used by all controlled endpoints.
