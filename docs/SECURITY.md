# Security

Implemented:
- extension allow-list;
- upload size limit;
- generated internal filenames;
- immutable source uploads;
- bearer authentication for controlled routes;
- no execution of uploaded content.

The demo token/admin password are development-only defaults and must be replaced for production.
